# ============================================================
# robots.txt — Scam Website Checker
# Drop this file in: client/public/robots.txt
# Then rebuild client so Vite copies it to dist/
# ============================================================
#
# RULES SUMMARY:
#   - Allow full crawl of the marketing pages + the homepage tool
#   - Disallow internal API endpoints
#   - Disallow synthetic error routes (404/500 — never useful in SERPs)
#   - Point crawlers to the canonical sitemap
#
# Replace https://scamwebsitechecker.com/ below with your real domain
# before deploying. Use absolute URLs in Sitemap directive.

User-agent: *
Allow: /
Allow: /about
Allow: /privacy
Allow: /terms
Allow: /contact
Allow: /how-it-works

# Internal API — never index
Disallow: /api/

# Synthetic error pages — not useful in SERPs, waste crawl budget
Disallow: /404
Disallow: /500

# Block common asset scrapers / AI training bots (optional, safe to remove)
User-agent: GPTBot
Disallow: /

User-agent: CCBot
Disallow: /

User-agent: anthropic-ai
Disallow: /

User-agent: ClaudeBot
Disallow: /

User-agent: PerplexityBot
Disallow: /

Sitemap: https://scamwebsitechecker.com/sitemap.xml